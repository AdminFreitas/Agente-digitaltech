# Backlog de agentes

Esboços escritos mas ainda não conectados a nenhum pipeline (nenhum import no projeto referencia estes arquivos). Ficam aqui como referência para quando fizer sentido implementar de verdade, em vez de misturados com os agentes que já rodam em produção.
